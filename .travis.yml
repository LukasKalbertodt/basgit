language: rust
rust:
  - nightly

before_install:
  - npm install -g less

env:
  - RUSTFLAGS="--deny warnings"

script:
  - ci/check-basic-style.sh
  - cargo build --verbose
  - cargo test --verbose

cache: cargo
